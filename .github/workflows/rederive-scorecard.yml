name: Re-derive scorecard from real closes

# Operator tool for the one remaining piece of the #605 fix.
#
# #605 stopped the back-check writing `price_next_day` from an in-progress
# daily bar. It did NOT repair the rows already published — and /scorecard's
# hit rate, median alpha and JSON-LD Dataset markup are computed from them.
#
# This runs `app.scripts.rederive_scorecard` ON THE FLY MACHINE, which is the
# only place that has both the database and the vendor key. (It cannot run from
# a Windows dev box at all: psycopg refuses the ProactorEventLoop.)
#
# ── READ THIS BEFORE CHOOSING `apply` ────────────────────────────────────────
# COST. A full pass is roughly 790 vendor calls => ~2.6 HOURS at the 12s
# default pacing (5 calls a minute). That pacing is the script's own default,
# not the plan's limit: 5 calls a minute is the vendor's free Basic tier, and
# its pricing page lists Stocks Starter, the plan in use, with unlimited API
# calls (massive.com/pricing, read 2026-09-19). The SAME key serves the live
# worker's price snapshots, daily-bar aggregates, news, crypto rows and
# scorecard back-check, so every call this job makes shares the key with the
# worker. (Fundamentals, analyst ratings and the sector backfill use Finnhub,
# and insider Form 4 comes from SEC EDGAR, not this key.)
#
# Two ways to make that acceptable:
#   * Use `since` to repair in chunks (a month at a time), off-hours.
#   * Or pass a smaller `pace` (no plan change is needed), which turns the
#     whole job into minutes. Try it on a short `since` window first, because
#     the key is shared with the live worker.
#
# Run `estimate` FIRST — it reads the DB, makes zero vendor calls, and prints
# the exact call count and wall-clock for the window you chose.
#
# ORDER OF OPERATIONS
#   1. mode=estimate            — cost only, no vendor calls, no writes
#   2. mode=dry-run             — real fetches, reports before/after, NO writes
#   3. mode=apply               — writes. Irreversible; rewrites the public record.
#
# NO SILENT REWRITE. apply needs `restatement`: the date of an entry ALREADY
# merged and deployed in backend/app/services/scorecard_export.py RESTATEMENTS
# (the dated list that ships inside the downloadable record). The script refuses
# otherwise, and only writes rows recorded before that date.

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "estimate = cost only | dry-run = fetch + report | apply = WRITE"
        required: true
        default: estimate
        type: choice
        options:
          - estimate
          - dry-run
          - apply
      since:
        description: "Only repair dates >= YYYY-MM-DD (blank = all). Use this to chunk."
        required: false
        default: ""
      pace:
        description: "Seconds between vendor calls. 12 = 5/min, the script default (not a plan limit; the key is shared with the live worker)."
        required: false
        default: "12"
      slice_days:
        description: "Days per ssh session (0 = one long session). 30 keeps each slice well inside the ~45min the stream survives."
        required: false
        default: "30"
      restatement:
        description: "For mode=apply ONLY: YYYY-MM-DD of the deployed RESTATEMENTS entry that discloses this rewrite"
        required: false
        default: ""
      confirm:
        description: "For mode=apply ONLY, type exactly: rewrite-the-public-record"
        required: false
        default: ""

concurrency:
  group: rederive-scorecard
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    # A full pass at the default 12s pace is ~2.6h; leave headroom but stay under the
    # 6h job ceiling so a wedged run fails loudly rather than hanging.
    timeout-minutes: 330
    steps:
      - name: Gate the destructive mode behind an explicit phrase
        if: ${{ inputs.mode == 'apply' && inputs.confirm != 'rewrite-the-public-record' }}
        run: |
          echo "::error title=Not confirmed::mode=apply rewrites the PUBLIC track record."
          echo "Type 'rewrite-the-public-record' in the confirm box, and run"
          echo "mode=dry-run first so you have seen the before/after numbers."
          exit 1

      # Pinned to the same immutable SHA as every other workflow that holds the
      # production Fly token.
      - name: Setup flyctl
        uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1 # 1.6

      - name: Run the re-derivation on the Fly machine
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          MODE: ${{ inputs.mode }}
          SINCE: ${{ inputs.since }}
          PACE: ${{ inputs.pace }}
          SLICE_DAYS: ${{ inputs.slice_days }}
          RESTATEMENT: ${{ inputs.restatement }}
        run: |
          set -euo pipefail
          if [ -z "${FLY_API_TOKEN:-}" ]; then
            echo "::error title=No token::FLY_API_TOKEN secret is not set."
            exit 1
          fi

          args=""
          case "$MODE" in
            estimate) args="--estimate" ;;
            dry-run)  args="" ;;
            apply)
              if ! printf '%s' "${RESTATEMENT:-}" | grep -Eq '^[0-9]{4}-[0-9]{2}-[0-9]{2}$'; then
                echo "::error title=No restatement::mode=apply needs the YYYY-MM-DD of the deployed RESTATEMENTS entry that discloses this rewrite."
                exit 1
              fi
              args="--apply --restatement ${RESTATEMENT}" ;;
            *) echo "::error::unknown mode $MODE"; exit 1 ;;
          esac
          [ -n "${PACE:-}" ]  && args="$args --pace ${PACE}"
          # The sliced path appends its own --since/--until per slice, so keep
          # a copy of the flags WITHOUT the operator's --since to build on.
          sliceargs="$args"
          [ -n "${SINCE:-}" ] && args="$args --since ${SINCE}"

          echo "mode=$MODE  args='$args'"
          echo "--------------------------------------------------------------"
          # -C runs one command and streams its output back. A FULL pass at the
          # default pace is ~2.6h and the stream does not survive that: an earlier run
          # was torn down at ~45 minutes with "ssh shell: wait: remote command
          # exited without exit status", which left the PUBLIC record half
          # repaired and no record of where it stopped.
          #
          # So the window is walked in slices, one ssh session each. Every row
          # the script touches is idempotent (a row already matching the vendor
          # is counted `same` and not rewritten), so a slice that dies is just
          # re-run, and every completed slice is durable. `estimate` makes no
          # vendor calls at all, so it always runs as a single session.
          if [ "$MODE" = "estimate" ] || [ "${SLICE_DAYS:-0}" -le 0 ]; then
            flyctl ssh console -a tapeline-backend \
              -C "python -m app.scripts.rederive_scorecard ${args}"
            exit 0
          fi

          # The public scorecard starts 2026-05-18; `since` narrows it further.
          cur="${SINCE:-2026-05-01}"
          end=$(date -u +%Y-%m-%d)
          slice=0
          failed=0
          while [ "$(date -u -d "$cur" +%s)" -le "$(date -u -d "$end" +%s)" ]; do
            stop=$(date -u -d "$cur + $((SLICE_DAYS - 1)) days" +%Y-%m-%d)
            slice=$((slice + 1))
            echo ""
            echo "=============================================================="
            echo "SLICE $slice:  $cur .. $stop"
            echo "=============================================================="
            # A torn-down slice must not abort the remaining ones — the run is
            # resumable by design and the later dates are independent. Record
            # it and fail the job at the end so it cannot be read as a clean
            # pass.
            if ! flyctl ssh console -a tapeline-backend \
              -C "python -m app.scripts.rederive_scorecard ${sliceargs} --since ${cur} --until ${stop}"; then
              echo "::warning title=Slice failed::$cur..$stop did not complete; re-run this window."
              failed=$((failed + 1))
            fi
            cur=$(date -u -d "$stop + 1 day" +%Y-%m-%d)
          done
          echo ""
          echo "slices run: $slice   failed: $failed"
          [ "$failed" -eq 0 ] || { echo "::error::$failed slice(s) failed"; exit 1; }

      - name: Confirm the public scorecard still serves
        if: ${{ inputs.mode == 'apply' }}
        run: |
          set -euo pipefail
          code=$(curl -s -o /tmp/sc.json -w "%{http_code}" --max-time 30 \
            "https://api.tapeline.io/api/scorecard?cb=${GITHUB_RUN_ID}")
          echo "GET /api/scorecard -> $code"
          [ "$code" = "200" ] || { echo "::error::scorecard is not serving"; exit 1; }
          python3 - <<'PY'
          import json
          d = json.load(open("/tmp/sc.json"))
          s = d.get("summary", {})
          print("published after the repair:")
          for k in ("days_tracked", "entries_scored", "entries_excluded_outliers",
                    "hit_rate_beat_spy", "median_alpha_vs_spy", "avg_alpha_vs_spy"):
              print(f"  {k}: {s.get(k)}")
          PY
