[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    real_svix: tests that exercise real Svix signature verification (skipped by the bypass fixture in tests/test_inbox_webhook.py)
